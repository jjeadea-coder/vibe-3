[project]
name = "public-sector-admin-superapp-backend"
version = "0.1.0"
description = "FastAPI backend scaffold for public-sector administration superapp"
requires-python = ">=3.14"
dependencies = [
  "fastapi==0.138.2",
  "uvicorn==0.49.0",
]

[tool.uv]
package = false
